@switch (Type)
{
    case "AdministrationTitle":
        <span class="selected-font">@Title</span>
        break;
    default:
        break;
}
@code {
    [Parameter] public string Title { get; set; }

    [Parameter] public string Type { get; set; }
}
